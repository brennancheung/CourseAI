import { defineSchema } from 'convex/server'

// Session logs are stored as Markdown files in src/data/sessions/
// Convex is available for future features that need reactive UI or complex queries

export default defineSchema({
  // No tables yet - add as needed
})
